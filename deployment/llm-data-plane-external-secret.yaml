# Reference-only local Quick Start manifest; it contains no credential value.
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: telecom-triage-llm-provider
  namespace: dp-default-default-default-ccb66d74
  labels:
    app.kubernetes.io/name: telecom-incident-triage
    app.kubernetes.io/component: llm-runtime
spec:
  refreshInterval: 15s
  secretStoreRef:
    kind: ClusterSecretStore
    name: default
  target:
    name: telecom-triage-llm-provider
    creationPolicy: Owner
    deletionPolicy: Retain
    template:
      type: Opaque
  data:
    - secretKey: api_key
      remoteRef:
        key: secret/default/generic/telecom-triage-llm-provider
        property: api_key
